These files are used to generate hand coded C that you can use to build
your own models.

Copy the files ... to your directory

Edit the files:
* Makefile.mdl
* mdl.c
* mdl_rtb.h
* mdl_rtp.h

To generate
# make -f Makefile.mdl
